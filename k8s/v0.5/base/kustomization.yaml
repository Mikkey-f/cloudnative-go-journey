apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 基础资源
resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml

# 通用标签
labels:
  - pairs:
      app: api
      version: v0.5

# 默认命名空间
namespace: default
