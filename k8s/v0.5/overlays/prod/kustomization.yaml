apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# 基础配置
resources:
  - ../../base

# 环境标识
namePrefix: prod-

labels:
  - pairs:
      env: prod

# 资源补丁
patches:
  - path: deployment-patch.yaml
  - path: configmap-patch.yaml
